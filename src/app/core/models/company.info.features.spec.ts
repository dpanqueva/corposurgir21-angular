import { CompanyInfoFeatures } from './company.info.features';

describe('Company.Info.Features', () => {
  it('should create an instance', () => {
    expect(new CompanyInfoFeatures()).toBeTruthy();
  });
});
